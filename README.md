# NUL Headcount Attendance System

## Setup
1. Install Node.js and MySQL
2. Run `npm install` in both `/` and `/headcount-server`
3. Create a `.env` file in `/headcount-server` with your database URL
4. Run `npx prisma db push` and `npm run db:seed`
5. Start both servers with `npm run dev`
